#include <bits/stdc++.h>

using namespace std;

int main ()
{
    int x, y;
    cin >> x >> y;
    string nothing[x];
    for (int i = 0; i < x; i++)
        cin >> nothing[i];
    cout << y;
    
    
    return 0;
}
