#include <bits/stdc++.h>

using namespace std;

int main ()
{
    long double area;
    cin >> area;
    long double a = sqrt(area);

    cout << fixed << setprecision(6);
    cout << 4 * a;

    return 0;
}
